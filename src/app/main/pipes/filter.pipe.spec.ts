import { FilterPipeStore } from './filter.pipe';

describe('FilterPipeStore', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeStore();
    expect(pipe).toBeTruthy();
  });
});
